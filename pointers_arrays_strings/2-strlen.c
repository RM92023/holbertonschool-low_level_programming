#include "main.h"
#include <string.h>
/**
 * _strlen - return length of a string
 * @s: parameter
 * Return: character or length of characters
 *
 */

int _strlen(char *s)
{
size_t length = 0;
length = strlen(s);
return (length);
}
