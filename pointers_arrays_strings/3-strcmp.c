#include "main.h"
#include <string.h>
#include <stdio.h>
/**
 * _strcmp - compare two strings
 * @s1: parameter
 * @s2: parameter
 * Return : compare two strings
 *
 */
int _strcmp(char *s1, char *s2)
{
char r = strcmp(s1, s2);
return (r);
}
