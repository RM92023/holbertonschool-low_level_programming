#include "main.h"
#include <string.h>
/**
 * _strcmp - compare two strings
 * @s1: parameter
 * @s2: parameter
 * Return : compare two strings
 *
 */
int _strcmp(char *s1, char *s2)
{
int result;
result = strcmp(s1, s2);
return (result);
}
