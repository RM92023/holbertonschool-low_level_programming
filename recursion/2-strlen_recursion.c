#include "main.h"
#include <stdio.h>
#include <string.h>
/**
 * _strlen_recursion - return length of a string
 * @s: parameter
 * Return: 0
 *
 */
int _strlen_recursion(char *s)
{
int n;
n = strlen(s);
return (n);
}
