#include <stdio.h>
/**
 * main - Print ten numbers only one digit
 *
 * Return: Always 0.
 *
 */
int main(void)
{
int log;
for (log = 0; log <= 9; log++)
{
printf("%d", log);
}
printf('\n');
return (0);
}
