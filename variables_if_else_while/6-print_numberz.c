#include<stdio.h>
/**
 * main - print numbers only 0-9 whitout use chart
 *
 * Return: Always 0.
 *
 */
int main(void)
{
int log;
for (log = 0; log < 10; log++)
{
putchar(log)
}
putchar('\n');
return (0);
}
