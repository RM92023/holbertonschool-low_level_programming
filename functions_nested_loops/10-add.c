#include "main.h"
/**
 * add - add between a number with other number
 * @number1: first number
 * @number2: second number
 * Return: add between two numbers
 *
 */
int add(int number1, int number2)
{
return (number1 + number2);
}
